<?php
/*
require_once("Sidney_DAO.php");
$dao = new SidneyDAO();
$todos = $dao->selectAll();
$contador=0;
foreach($todos as $objeto) {
    $json [] = array("id" => $objeto->getId(), "Tiempo" => $objeto->getTime(), "Descripcion" => $objeto->getDes());
}

header('Content-type: application/json');
echo json_encode($json);
*/
?>